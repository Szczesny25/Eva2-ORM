hola

para iniciar, una vez ya tienes el repo descargado te metase a la terminal y coloque:

composer install (si no esta instalado pero raro que no)

composer update

copy .env.example .env(windows(fome))
cp .env.example .env(Linux(genio))

php artisan key:generate
php artisan migrate (seguramente diga que no hay nada que migrar pero como que me da algo si no lo coloco)

npm install
npm build

y por ultimo

npm run dev
php artisan serve

y ya estaria

cualquier error contacte al siguiente correo:

holasoysordociegomudo@hotmail.uz

lo contestaremos entre 1 a 5 años habiles muchas gracias
si no le contestamos envie de nuevo el correo 